using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Storage : MonoBehaviour
{
    // Start is called before the first frame update
    public ArrayList storedGoods;
    void Start()
    {
        ///Stone
        ///wood
        ///wheat
        ///clay
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
